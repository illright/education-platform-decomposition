/**
 * Make a request to the API and parse the result.
 *
 * Result type is automatically inferred from the endpoint.
 */
export function fetchFromApi<ResultType>(endpoint: string, options: FetchOptions) {

}
